defmodule Writer.SingleTest do
  @moduledoc false
  # use ExUnit.Case

  # alias ExWal.LogWriter

  # @path "./tmp/log_writer_test"

  # setup :cleanup

  # defp cleanup(_) do
  #   File.rm_rf(@path)
  #   :ok
  # end

  # setup_all  do
  #   start_supervised(LogWriter.Single, {})
  #   {:ok, writer: nil}
  # end
end
